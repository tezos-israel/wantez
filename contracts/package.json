{
  "name": "contracts",
  "version": "1.0.0",
  "dependencies": {
    "@taquito/taquito": "^8.1.0",
    "eslint-plugin-truffle": "^0.3.1",
    "promise-retry": "^2.0.1",
    "truffle": "^5.5.0-tezos.4"
  },
  "scripts": {
    "start-sandbox": "docker run --rm --name my-sandbox --detach -p 20000:20000 tqtezos/flextesa:20210216 edobox start",
    "stop-sandbox": "docker kill my-sandbox",
    "test": "truffle test",
    "compile": "truffle compile",
    "migrate": "truffle migrate",
    "clean": "rm -rf ./build"
  },
  "keywords": [
    "tezos",
    "blockchain",
    "truffle"
  ],
  "license": "MIT"
}
