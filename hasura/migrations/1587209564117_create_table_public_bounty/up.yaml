- args:
    cascade: false
    read_only: false
    sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
  type: run_sql
- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "public"."bounty"("title" varchar NOT NULL,
      "id" uuid NOT NULL DEFAULT gen_random_uuid(),
      "issue_url" text NOT NULL, "description" text NOT NULL, "fee" numeric
      NOT NULL, "funder_id" uuid NOT NULL, PRIMARY KEY ("id") , UNIQUE ("title"),
      UNIQUE ("id"), UNIQUE ("issue_url"));
  type: run_sql
- args:
    name: bounty
    schema: public
  type: add_existing_table_or_view
